info:
  name: redirect
  type: http
  seq: 4

http:
  method: GET
  url: https://httpbin.org/redirect/3
  auth: inherit

runtime:
  assertions:
    - expression: sdfvf
      operator: eq
      value: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

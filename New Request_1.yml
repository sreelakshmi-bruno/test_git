info:
  name: New Request_1
  type: http
  seq: 3

http:
  method: POST
  url: https://echo.usebruno.com
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        // 1. Check if pm itself is what you think it is
        console.log(typeof pm);
        console.log(JSON.stringify(pm));

        // 2. Check what's actually on pm
        console.log(Object.getOwnPropertyNames(pm));

        // // 3. Is pm.require even a function?
        // console.log(typeof pm.require);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
